-bt=DOS -l=dos4g -fpd -mf -oneatx -oi -ol+ -ot -we -wx -za -zp4   -zc   $ProjectFileDir$\main.c

To generate debug info (I won't include it above so it's not a waste of space (and a mess for me to get where the actual segment ends and starts unnecessary debug info which I don't even use to debug this):
-d3 -db
